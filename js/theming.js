// js/theming.js — Cambric AI uses dark-only Tokyo Night theme
// No light/dark toggle needed — the site is dark by design
(function(){
  // Ensure dark mode is always active
  document.documentElement.classList.remove('light');
})();
