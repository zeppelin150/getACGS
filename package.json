{
  "name": "cambric-ai-website",
  "version": "1.0.0",
  "private": true,
  "description": "Cambric AI marketing website with visual regression testing",
  "scripts": {
    "test:visual": "npx playwright test",
    "test:visual:update": "npx playwright test --update-snapshots",
    "test:live": "npx playwright test --config=playwright-live.config.ts",
    "test:live:update": "npx playwright test --config=playwright-live.config.ts --update-snapshots",
    "figma:sync": "node scripts/figma-sync.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
